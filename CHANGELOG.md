# 更新日志

只记当前版本。更早的改动看 git 历史或 Release 页。

## v2.16.3 — 把 v2.16.2 删掉的东西放回上下文，只留下三条约束

v2.16.0/2.16.1/2.16.2 那三轮把「上下文里不许塞句子」执行成了**把内容一起铲掉**：体感句、
话的份量、夜间语气、情绪态度句、语气表、行为倾向、边界句与许可句，连生成它们的代码都删了。
这是错的——用户要的是「别替她说话、别限制她」，不是「别告诉她她自己的身体」。本版把内容
全部放回来，只保留三条真正的约束：**不写台词、不下禁令、不给读数**。

### 回到上下文里的东西（`humanoid/prompt_builder.py`）

| 块 | 内容 |
|---|---|
| 【我的感觉】 | `soma.feelings()` 的第一人称体感，按显著度门控（默认档最多两句） |
| 【话的份量】 | `soma.form_policy()` 算出的份量，写成状态而非规矩：「这副身体这会儿只够说一两句，20字上下的量」 |
| 【对TA】 | 情绪标签 + 态度句（「你这两天积了点火，现在对TA有点意见」）+ 心气表 `MOOD_TONE_HINTS` |
| 夜间两档 | `build_night_lines()` 回来，`night_mode_force_sleep` 配置项一并恢复（config + schema） |
| 【刚刚】 | 行为倾向 `BehaviorService.compute_agency()` 整个算法恢复（主动/好奇/关心/社交意愿/延续话题） |
| 收尾两句 | `BOUNDARY_LINE`（这不是要你汇报的表格，也不是要你照着念的台词）+ `PERMISSION_LINE`（说多长、怎么接，你自己定） |

三档 `inject_activity_context` 恢复成原来的详略差别：`low` 门控体感、`full` 全量状态、
`mood_only` 只给关系与一句感觉；字符上限回到 640 / 900 / 330。实测 low ≈344 token
（身体推到极端 ≈379）、full ≈358、mood_only ≈210。

### 保留下来的三条约束（这三样仍然被删着，测试逐项卡住）

- **台词**：「被吵醒就回一句『我现在需要休息，明天再聊吧』，不要接着聊」——插件替她把话说完。
  夜间两档现在只讲身体是什么状态；语气表从「语气柔和，略带撒娇」改成「想黏着TA一点」。
  情绪态度句尾巴「说话会短、会顶回去」也不要了，只说她心里怎么样。
- **禁令**：「不要接着聊」「别超过60字」「这一轮不追问」「不应回复」——插件拦不住回复，
  这类句子只是让模型去猜规矩。她在睡觉时被吵到要怎么回是她自己的判断。
- **读数**：`好感46.0/100`、`社交能量100%`（改成「社交能量充足」这类词）、`UTC+08:00`
  一律不进上下文；要看数值去 `/她的状态`、`/好感度`、`/时间`、`/拟人诊断`。

`tests/test_v2151_freedom.py` 一并恢复：把身体推到极端（熟睡、饿、难受、被冷落）后渲染三档
注入，逐项断言没有命令式句子、没有台词、没有读数；`test_v216_facts.py` 里那批「断言内容不
在上下文里」的测试删掉（它们守的是错误方向），时区与地名的测试全部保留。

### 同时保留 v2.16.2 做对的事

- 时区：俄罗斯 5 个走快一小时的城镇已改（滕达/涅留恩格里/阿尔丹/奥廖克明斯克 →
  `Asia/Yakutsk`，恰拉 → `Asia/Chita`）、补上比利比诺与四个漏掉的主体首府、修掉两条 tzdata
  里不存在的 IANA 名；表 781 条（中 452 / 俄 205 / 日 124）。
- 地名怎么写都认得：先精确匹配再去行政后缀（`广东省`、`大阪市`、`东京都`、
  `延边朝鲜族自治州`），中国 34 省级、日本 47 都道府县、俄罗斯联邦主体名进表。
- 间隔报准确时长：「距上次说话 3 小时 12 分」，不再用「对方隔了约2～6小时重新出现」这类
  档位话术；【此刻】那句补上钟点与节日。

测试 **335 项**全绿（24 项为 `main.py` 集成测试）。
