# 更新日志

只记当前版本。更早的改动看 git 历史或 Release 页。

## v2.16.4 — 事实进消息、读法进 system prompt；地区表补到县级；日程与身体换了方向

上一版把「别替她说话」执行成了「把她少说」。这一版把两件事彻底分开：
**她的身体、日子、注意力**照样算、照样给；**台词、禁令、读数、编造的细节**一样都不给。
另外把「参考该放哪儿」这件事修对了——以前每条消息都往用户消息后面塞一块，而 AstrBot 存历史时
存的就是拼装后的用户消息，于是聊得越久历史里堆的过期身体状态越多。

### 上下文分了两处（`main.py`、`humanoid/prompt_builder.py`）

- **事实块**仍走 `extra_user_content_parts`，但每次注入前先把上下文里旧的块抹掉（块首有
  `〔她的身体与生活 v4 uid=…〕` 标记，字符串形式与多块形式都认）。全程只有一份，且是新鲜的。
- **「这些事实该怎么读」进 `req.system_prompt`**（`FRAMING_TEXT`，200 字 ≈196 token）。
  它不随消息变，所以一次说清就够；AstrBot 保存历史时跳过初始 system 消息，不会滚雪球。
  这就是你要的「参考不是硬加到 Context，而是让模型知道该怎么理解」。
- 三档实测：`low` 私聊 125 token / 群聊 74；`full` 159 / 108；`mood_only` 108 / 58
  （v2.16.3 是 344 / 358 / 210，且每条消息都在历史里多留一份）。
- 必需品三样**任何一档都在**：时间（含她的钟点、星期、节日）、场景（私聊还是群聊）、称呼。
  没记下称呼时直说「还没问过TA叫什么」，而不是不提。

### 删掉的替她说话与限制她的东西

- 「你还在睡，**是手机震醒的那种**：眼睛睁不开，脑子没转，话到嘴边只剩一两个字」——手机、
  眼睛、脑子都不是算出来的，是插件替她写的人设。夜间现在只剩事实：
  「这段时间她自己排的是觉」/「按她自己的节律，现在是这一天里最该睡的时候」。
- **【话的份量】整块从上下文删除**：「这副身体这会儿只够说一两句，20字上下的量」「话到嘴边也就
  40字」——她在睡觉时被规定只能说 20 字，那是限制不是参考。`soma.form_policy()` 照算、照样
  通过契约给社交层用，只是不再进她的聊天上下文。
- 「这是一次性的背景，不是每天要重新问候的事」「（以上只是背景，不必逐条回应…）」这类指导句删了，
  它们改住在框架句里，只说一次。
- `night_mode_force_sleep` 换成 `show_sleep_window`：旧键的含义是「睡着时把状态说得多硬」，
  那已经是替她决定怎么开口。新项只管**这条事实给不给**（默认给），关掉不影响她身体照算。
- 精力描述里的「语气轻快 / 语气低落 / 语气平和」全删：那是插件在教她用什么语气说话。

### 间隔不再是秒表（`【刚刚】`）

三段式经历，全部是事实：**TA那句原话** + **距那之后过了多久**（准确时长）+ **这期间她自己干了什么**
（从今天日程的时段算，跟【今天】那块不重复）。旧版只有一句「距上次说话 3 小时 12 分」，
她看不出这段时间是怎么过去的。「TA是不是去忙了」这种念头不写进去——那是她自己该生出来的。

### 注意力与兴趣：三条轴，数值各人不同（`humanoid/services/behavior.py`）

- `care` 在意度：对**这个人**。慢变量，72 小时半衰期往目标逼近；目标 = 好感度 + 脾气 +
  一个按（角色,用户）稳锚的基线错位（±0.22）。以前所有人第一面都是同一个 50 分。
- `focus` 上心程度：对**TA正在讲的这件事**。每条现算，不写盘——问句/语气词、长度、
  是否推到她今天在忙的事、是否推到 TA 自己之前说过而她记着的事、心情调制。
- `spare` 注意力余量：**她自己手里还剩多少**，跟 TA 无关。困、难受、饿、在睡、手上正忙、精力低。
- 三轴进上下文的是措辞（「跟TA挺熟」/「这事你听进去了」/「这会儿你腾不出多少注意力」），
  数值只在 `/你的状态` 里看：`- 注意力：在意 62%、上心 45%、余量 70%`。
- 群聊没开成员情绪档案时三轴不记账，也不会顺手给成员建条目。

### 措辞不再是一份稿子（`humanoid/wording.py`）

程度由轴值挑（`scale_word`），同一档位备 2~3 套等价说法，按（角色 + 今天 + 用户 + 档位）抽签
（`pick`）。同一天里她不会把同一句话说两遍，隔天、换角色、换人就是另一套；措辞从数值/时长/
事件名拼出来，不是整句硬编码。体感、心气、间隔说法、精力档位都走这一层。

### 日程与身体换了方向：人格决定作息

- 日程 prompt 不再写「她的作息必须遵守：23:00 上床，生物钟夜到 6:00 结束」，改成
  「几点睡由她是谁决定：从人设里读她的年纪感、工作或学业、生活习惯；夜猫子就排凌晨睡晚起」。
- `schedule_follow_night_window` 默认**关**（打开才是硬覆盖，代码仍会把睡眠切到窗口上）。
  从没碰过作息设置的老用户会被一次性迁移到新默认（碰过的不碰）。
- **身体反过来跟着日程走**：`soma.biological_night()` 从今天日程里最长那段觉（会自动合并
  23:30→24:00 + 00:00→07:00）推出她的昼夜低谷、睡眠债记账边界与窗口长度。夜猫子凌晨 4 点睡、
  11 点半起，低谷就跟着挪到 7 点半前后，不再被 23→6 那套固定窗口按住。
- 契约 `routine` 导出的 `night_start_hour` / `night_end_hour` 现在就是**她实际的**生物钟夜
  （新增 `night_source`、`configured_night` 说明来源）。字段名与契约版本没动，社交层不用改。
- 内置模板里「夜间洗漱准备睡觉」「准备入睡」「午休或娱乐」这类会被当成睡眠的名字改了，
  否则她下午三点也算在睡、晚上九点就起不来。

### 地区与时区补全（`humanoid/data/cities_bulk.py`，新文件）


- 地名表 **781 → 9,855 条**（其中 GeoNames 批量表 9,411 条，与手写表去重后合并）。批量部分由 GeoNames cities500 生成：
  中国县级及以上驻地（或人口 ≥2 万）5,142 条、日本市町村 3,315 条、俄罗斯有中文名且人口
  ≥1 万的居民点 954 条，覆盖 108 个 IANA 时区；
  繁体/日文写法（盛岡、軽井沢、摩爾曼斯克）另转一层简体别名，两种写法都认。
- **俄罗斯 85 个联邦主体名全部进表**（裸名 + 官方写法，共 165 条），时区按首府推：
  `斯维尔德洛夫斯克州`、`萨哈林州`、`堪察加边疆区`、`印古什共和国`、`犹太自治州` 这些以前
  一律认不出。
- **中文显示名可以填回去**：`IANA_DISPLAY_NAMES` 反向进表，她说「你在雷克雅未克」，你把
  「雷克雅未克」填进去就生效（以前填不回去，静默退回机器时间）。
- 跨国重名（白山、中山、松江、海南…）留人口多的那个；俄罗斯内部同名跨两个时区的整条丢掉——
  宁可认不出并明说，也不给一个错钟点。
- 本轮实测补上的具体缺口：敦煌、满洲里、乌兰浩特、曲阜、阳朔、婺源、都江堰、井冈山、平遥、
  箱根、轻井泽、八户、维堡、佩韦克，以及 85 个主体名。
- 认不出的地名照旧**不静默退化**：`/拟人诊断`【时间与时区】会打 ⚠ 并说她现在按这台机器的时间过日子。

### 使用面变轻（代码变重了，配置不用翻）

- 面板 82 项重排：前 8 项是「城市、日程用人设、大模型日程、额外偏好、上下文详略、参与环境、
  管理员、调试日志」，其余 74 项描述前缀【进阶】（AstrBot 面板不支持分组，只能靠顺序和标题）。
- 新增 **`/拟人设置`**：不带参数列出这 8 项当前值；`/拟人设置 城市 大阪`、`/拟人设置 上下文 full`、
  `/拟人设置 人设 关` 这样改一项，写完立即生效（含写回配置文件；写不进去会明说）。枚举与布尔
  认不出来会被挡下，非管理员改不了。
- 新增文件只有 `humanoid/wording.py`（措辞层）与 `humanoid/data/cities_bulk.py`（数据）。

### 验证

- 单元 **356 项**全绿（+10：地区表批量层 9 项 + 夜猫子契约 1 项）。
- 注入链冒烟 30 项（`/tmp/inject_v2164.py`：框架/事实分流、旧块清理、字符串与多块两种历史形态、
  群聊、`/拟人设置` 全路径）。入口冒烟 26 项、Core↔Social 接缝 15 项全绿。
- 守护测试 `tests/test_v2151_freedom.py` 扩了：三档注入渲染出来不许出现命令句、台词、读数、
  「手机/眼睛/脑子」这类编造细节；必需品三样每档都在；间隔必须是经历式三段。
