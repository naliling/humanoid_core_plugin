# 更新日志

只记当前版本。更早的改动看 git 历史或 Release 页。

## v2.16.7 — 动态日程交给模型填表，数值只做参考不替她说话

一句话：**插件只递数值，不递结论**。日程怎么排、话怎么说，都交还给模型；所有进上下文
与进 prompt 的数值都标明「是参考，不是条件」。

### 动态日程：模型填表，15 分钟按身体重排

- **身体数值进日程 prompt**：每次重排现取活值——精力、饥饿、睡眠压力、睡眠债、昨晚
  睡了多久、生理周期、当前时间，写进【她此刻的身体参考数值】块，并明确标注「是参考，
  不是必须满足的条件，怎么权衡由你决定」。数值是活的：饿了、困了、欠觉了，下一轮重排
  模型看得到。
- **删掉全部内容提示**：以前 prompt 里写着「要有真的会打断聊天的事」「别排成每分钟都在
  做有意义的事」「『改第三季度的报表』比『上班』有用」——那是插件替模型思考。现在只递
  三样东西：她是谁、她此刻的身体参考数值、表格长什么样。
- **15 分钟重排**：新增 `schedule_refresh_minutes`（默认 15）。跨天立即排，平时到了间隔
  就按新身体数值重排一次当天日程；调大可省模型额度，1440 = 一天一次。
- **96 格填满 24 小时**：`schedule_max_slots` 默认 16→96，prompt 要求「把 24 小时每一格
  都填满」，模型可以逐格填也可以合并连续几格，粒度自己定。
- **失败退避补了一刀**：「到点重排」不绕过失败退避——模型挂掉时「到点」永远成立，不挡
  会退回每 30 秒一次的永久重试（v2.14 修过的坑差点借尸还魂）。只有 `/重置日程` 和跨天
  可以硬闯。
- **额外偏好改成均衡参考**：默认文案换成「劳逸结合，有动有静，节奏均衡。」，并标注
  「仅供参考，与人设和身体状态权衡，不必逐字执行」。

### 只给数值，不限制发言

- **删掉情绪语气括号**：以前「对TA的感觉：亲密（态度亲昵）」「敌视（带有敌意）」——
  括号里那句是在教她用什么语气开口。现在只给标签本身，怎么说出来由她自己定。
- **框架句改成参考边界**：system_prompt 里那段「她的身体与生活」现在明说「作为背景参考；
  如何回应、说什么、说多长，由她自己判断」。
- **情绪标签去病态词**：痴迷→着迷、占有→独占、痴缠→难舍、虐恋→浓烈、驱逐→抗拒——
  极端数值不再配极端措辞，免得模型照着词把人设演过头。
- **心情标签多样式**：`/情绪详情` 的心情标签（如「有点疲惫，开心，想贴贴」）每档 3~4
  条等价说法随机换，同一个状态不再每次念同一句。标签只给人看，不进聊天上下文。

### 间隔的实用性

「TA有 3 小时 12 分没吭声了」只是秒表读数；`with_last_msg` 模式下同时给「离开前说的
最后一句：「我先去开会」」，模型自己就能接上「会开完了吗」。只给事实，不写「要主动问起」。

### 配置描述修正（面板里能直接看懂）

- `night_start_hour` / `night_end_hour` 不再说「也是她该入睡/该起床的时间」——几点睡几点
  起由日程决定，这两项只是生物钟夜的边界（睡眠债记账、昼夜低谷位置）。
- `social_energy_consumption_per_msg` 的「高频选0.05」改成正常说明。
- 新增 `schedule_refresh_minutes`、重写 `schedule_max_slots` / `schedule_prompt_extra` /
  `use_llm_schedule` / `schedule_time_granularity` 的描述，写明默认值含义与省额度方法。

### 顺带修掉的两个真缺陷（集成测试首次跑起来才暴露）

- `/批量好感度` 静默丢弃非法值：`222:200` 被丢掉却报「已批量设置 1 个用户」。现在会
  说「跳过 N 个（数值需在 0-100 之间）」。
- `/重置日程` 失败时不指路：现在失败消息带上「可用 /拟人诊断 查看模型配置」。
- 集成测试（`tests/test_main_integration.py`，23 项）从「永远 skip」变成真的在跑：
  移植了 astrbot 桩，并修正测试里臆想的旧 API（`engine.nickname()` 等从不存在的接口）。

### 老用户默认值迁移

AstrBot 更新配置只补缺不覆盖，所以插件自己迁：`schedule_max_slots` 仍是旧默认 `16` 时
提到 `96`；日程偏好仍是「休闲日常，愉快的生活。」时换成新的均衡参考。只动仍停在旧默认
值的项，用户自己改过的一律不碰（标记文件 `.defaults-migrated-v2.16.7`）。

### 验证

- 单元与集成 **367 项**全绿（skipped=1，仅剩需要真实 AstrBot 包的见证测试）。
- 新增回归：到点重排不绕过退避、96 格 prompt 填满要求、身体参考块「是参考不是条件」、
  补充偏好「仅供参考」、情绪标签无语气括号、批量好感度跳过提示。
