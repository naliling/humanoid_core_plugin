# 更新日志

只记当前版本。更早的改动看 git 历史或 Release 页。

## v2.16.5 — `/拟人诊断` 把「有没有对接上」说清楚（配合社交层 v1.9.0）

有用户反馈：装了自主拟人社交，`/拟人诊断` 却显示「没读到 humanoid_signals.json（未装自主拟人
社交，或它还没写过）」——跟真没装长得一模一样，分不清是插件没装、AstrBot 版本问题，还是
自己用 zip 导入的方式不对。**都不是**：是社交层那个信号文件在没有东西可报时永远不会被创建，
而 Core 这边把三种情况说成了一种。

### 这边改的：把一句话拆成四种状态，并打出它去看的路径

`humanoid/link.py` 新增 `SocialSignals.status()`，`humanoid/diagnostics.py` 与 `/你的状态`
按状态分别说：

| 状态 | 诊断会说 |
|---|---|
| 没装 | 「没装「自主拟人社交」插件。这不是故障——本插件单独也能跑」 |
| 装了但没写过 | 「装了自主拟人社交（它的目录在），但还没写过信号文件…」（并说明社交层 v1.8.2 起启动就会写） |
| 信号超时 | 「文件在，但已经 N 分钟没刷新（超过 15 分钟不采信）：社交层被停用、后台循环卡住，或刚重启还没跑一轮」 |
| 在用 | 「读到并在用。上次刷新 Ns 前；上次主动开口 Ms 前、冷落计数 K」 |

四种都会带上它实际去看的绝对路径（`<data>/plugin_data/astrbot_plugin_autonomous_social/
humanoid_signals.json`），目录名对不对一眼就能核。`/你的状态` 里也多一行
`- 自主拟人社交：已对接 / 未安装 / 装了但还没写过信号 / 信号超时未刷新`。

### 联动契约那行也重写了

以前只有一句「尚未生成（contract_enabled 关着，或身体还没推进过）」，把「配置关了」、
「生理层关了（这时故意不导出）」、「还没到第一次身体 tick」三种情况混在一起。现在分开说，
并写明契约是写在她自己的 `state.json` 里的。

### 另一处口径修正

`link.signals_path()` 加了 `must_exist` 参数（默认 `True`，所有旧调用方行为不变）：状态检查
需要拿到路径再自己判断文件在不在，而不是先被 `None` 挡掉。

### 验证

- 单元 **361 项**全绿（+5：`SignalsStatusTest` 覆盖 not_installed / never_written / stale /
  fresh 四种说法，以及 `/你的状态` 里那行）。
- 端到端复现（两个插件按 AstrBot 4.28 的目录布局同时跑）：Core 的 `state.json` 落地并含契约
  → 社交层启动即写信号 → Core 读到并在诊断里报「读到并在用（含路径）」。
- 社交层侧的根因修复在 autonomous_social **v1.8.2**（`signals.beat()`：启动写一次、每轮刷心跳）。
  两边都要升，只升 Core 的话诊断会准确地说出「装了但还没写过信号」。
