# 更新日志

只记当前版本。更早的改动看 git 历史或 Release 页。

## v2.16.1 — 上下文里只留事实，身体与生活退回数据层

（v2.16.0 是同一轮改动，只发了几分钟就被这张表的数据核对推翻了两条时区归属，所以直接并进来发；下面写的是这一版相对 2.15.x 的全部内容。相对 2.16.0 的差异只有：滕达/恰拉改按 `Asia/Yakutsk`、比利比诺改按 `Asia/Anadyr`，以及下面【时区】一节里核对范围的说明。）

这一版纠正一个方向性错误。README 第一屏就写着「这个插件负责给 AI 一具身体，不负责替 AI
说话」，而注入层里干的就是替她说话：夜间直接给出「我现在需要休息，明天再聊吧」让她照着发，
形式块写着「这一轮只说一两句，控制在20字上下」，情绪偏了要附一句「说话会短、会顶回去」，
语气表写着「语气柔和，略带撒娇」。v2.15.1 只是把这些句子换了个不那么像命令的说法——**问题
不在措辞，在于往上下文里塞句子本身**：模型不会把塞进去的东西当参考，只会当必须执行的任务，
那就不再是提示词参考，而是替她说话、替她思考、替她决定回复多长。

### 注入层：只剩事实

`humanoid/prompt_builder.py` 重写。三档 `inject_activity_context` 现在只决定**给多少生活
事实**，不再决定给多少指导：

- `low`（默认）：`【时间】`（日期、星期、几点、上午/晚上、她在哪个城市、群聊还是私聊）、
  `【称呼】`（你管TA叫谁）、`【刚刚】`（距上次说话多久、TA上一句说的是什么）。
- `full`：再加 `【今天】`（此刻在做什么、刚做过什么、接下来做什么）、`【TA说过】`（对方
  之前说过的原话）、`【天气】`。
- `mood_only`：只给一个关系档位词。

删掉的东西（都不再有开关，因为开关本身就是「想塞就塞」的口子）：

- **体感句**：`【我的感觉】眼皮很沉，句子不想写长` —— 身体状态不再被翻译成句子给她读。
- **形式约束**：`【话的份量】这一轮只说一两句，控制在20字上下`、`别超过60字`、`别展开成长篇`、
  `这一轮不追问`。她在睡觉时被吵醒，也不该被插件要求「必须 20 字以内回复」。
- **夜间语气与台词**：`night_mode_force_sleep` 配置项整个删除（AstrBot 会清掉面板里的旧值）。
  夜间窗口本身保留，但它只驱动身体（睡眠债、昼夜低谷、日程对齐），不再进上下文。
- **情绪态度句与语气表**：`_emotion_lines`、`MOOD_TONE_HINTS` 全删。情绪照旧计算、照旧影响
  她的行为与契约，只是不再被翻成「你对TA有意见，说话会短」这种句子。
- **行为倾向**：`BehaviorService.compute_agency` 删除（注入里那句「更主动、更好奇」就是它）。
- **元指令**：`BOUNDARY_LINE`/`PERMISSION_LINE` 那两句「这些是你的身体…你自己定」也删了——
  事实不需要免责声明，两句本身还是 160 个 token 的噪声。
- **给人看的技术量**：`UTC+08:00`、百分比、`好感46.0/100` 不再出现在上下文里。

身体与生活并没有消失：它们仍在 `state.json`、仍在联动契约（`body`/`form`/`feelings`/`day`
照旧导出给「自主拟人社交」，那边决定要不要开口与说多长）、仍在 `/你的状态`、`/查看日程`、
`/好感度`、`/拟人诊断` 里给人看。她说自己困不困、说多长，是模型从「凌晨两点」「她刚跑完
一整天」这些事实里自己幻想出来的。

实测体积：low ≈ 36 token、full ≈ 66、mood_only ≈ 20（v2.15 是 260~310）。字符上限跟着
收紧到 220 / 620 / 60。

### 时区：三个国家的地区全部收录，并且不许静默退化

`timezone_city` 决定她看到的钟点、日程几点做什么、夜间窗口、睡眠债从几点记、天气冷不冷。
这一版把它做成真的能落在任何一个地方：

- **723 条地区**：中国 425（全部地级行政区＋省直辖县级＋港澳台）、俄罗斯 179（27 个 IANA
  时区每一个都有城市，85 个联邦主体的首府与主要城市）、日本 110（47 都道府县厅所在地与
  政令市）。
- **拿 GeoNames cities500 完整文件（235,808 行，其中中日俄 23,568 行）逐条对过**：表内
  445 条能按中文名匹配上，其中 28 条与 GeoNames 的时区不同，逐条看过都有结论——17 条是
  新疆按北京时间的既定选择；4 条是港澳台/日本地名与别国村镇重名（GeoNames 按重名条目算）；
  汉德加按 IANA 自己的注释（`Asia/Khandyga` = "Tomponsky, Ust-Maysky"，汉德加正是该 ulus
  首府）比 GeoNames 更准；其余是偏移相同、区域归属不同。**据核对改正 2 条**：滕达
  （阿穆尔州，改 `Asia/Yakutsk`）、比利比诺（楚科奇，改 `Asia/Anadyr`）。
- **覆盖率**：把阈值一路降到人口≥1 万，中国仍只剩 9 个乡镇级重名条目未收（GeoNames 把
  整级人口挂在村名上的数据毛刺），俄罗斯与日本零缺失。
- **中国大陆（含新疆）统一北京时间**——铁路、电视、手机都这么走；想按当地 UTC+6 过就把
  配置填成 `Asia/Urumqi`。
- **补上 6 个此前完全没城市的时区**：`Europe/Volgograd`、`Europe/Astrakhan`、`Europe/Saratov`、
  `Europe/Ulyanovsk`、`Europe/Simferopol`、`Asia/Khandyga`（伏尔加格勒、阿斯特拉罕、萨拉托夫、
  乌里扬诺夫斯克、辛菲罗波尔、汉德加/涅留恩格里/奥廖克明斯克现在各归其位）。
- **补上漏掉的首府**：沃洛格达、普斯科夫、大诺夫哥罗德、维堡、佩韦克、比利比诺、埼玉、
  八户、费奥多西亚、阿赫图宾斯克、滕达、恰拉。
- **IANA 名可以直接填**，而且她说出来的是中文城市名：填 `Europe/Volgograd`，上下文里是
  「你在伏尔加格勒」，不会出现斜杠代码（`Clock.display_city` 以前会漏这一步）。
- **不再静默退回 `Asia/Shanghai`**：机器缺 tzdata 时旧逻辑会让「东京的她」过北京时间的一天。
  现在退回 AstrBot 所在机器的本地时间并说明原因。
- **填错与缺库分开报**：名字拼错说「这个时区名用不了（可能拼错了）」，只有真缺 tzdata 才
  提示装库——以前两种情况共用一句 tzdata 提示，把人往错方向引。
- **换城市会把旧账重新起算**（`core_instance._rebase_after_move`）：`last_update` 与
  `_last_weather_fetch` 存的是墙上时间字符串，读回来附的却是新时区，从东京改到北京会凭空
  多出几小时的精力漂移、天气也按错的时点算过期。
- **一个角色只有一个「现在」**：身体、情绪、事件记账共用 `clock.now().timestamp()` 这一个
  时间源（以前 soma 走 `time.time()` 而日程走城市钟，跨时区时「她在不在睡」会按宿主机小时
  算错）。
- 契约 `time.utc_offset_minutes` 算不出时导出 `None` 而不是 `0`（当成 0 等于把她的城市当
  UTC），新增 `time.tz`。`/拟人诊断` 新增【时间与时区】段，`/时间` 认不出城市时照样给时间
  但附上原因。

### 测试与文档

- 新增 `tests/test_v216_facts.py`：三档上下文在极端身体状态下仍不含任何命令式句子、体感句、
  字数要求、技术量；三国地区覆盖率、每个 IANA 时区可达、显示名存在、填错与缺库的提示分开、
  换城市重新起算、契约偏移导出。
- 全套 **311 项**（24 项为 `main.py` 集成测试）；插件入口冒烟 26 项、Core↔Social 接缝冒烟
  15 项另跑。
- README：【状态是怎么交给模型的】改写成【上下文里到底放什么：只有事实】，作息一节删掉
  夜间语气表，新增【城市与时区】一节；更新日志今后只写在本文件里，只写当前版本。

### 升级后会有什么不一样

- 她的回复不再被插件压成短句、不再夜里被赶人去休息——**如果以前那些效果你觉得「像人」，
  现在该把它写进 AstrBot 的人设里**，那是她自己的性格，而不是插件塞的句子。
- 上下文变小了约 5~8 倍（low 档从 ≈260 token 降到 ≈36），长对话里累积的注入噪声也随之消失。
- `night_mode_force_sleep` 已从配置面板消失；其余配置项一律保留原值。
