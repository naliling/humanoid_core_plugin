# 更新日志

只记当前版本。更早的改动看 git 历史或 Release 页。

## v2.17.1 — 日程真正动态：一次只决定一段，15 分钟一个「生成或不生成」的决策窗

上一版（v2.16.7）名义上「每 15 分钟按身体重排」，实际仍是**预制菜**：让模型一次性
填满 00:00→24:00 的整天表，把她一整天的未来在早上就写死。用户要的不是这个——要的是
**每一段现决定**：这一段过完了才决定下一段，中途要不要变由她当时的身体和心情说了算，
生成或不生成是概率性的。这一版把它改成真正的滚动分段。

### 不再预排未来，一次只决定「从现在起的这一段」

- 生成请求从「填整天表」改成「填一格」：prompt 里递上一段在做什么、做了多久、今天
  已经过完的时段、以及她此刻的身体读数，模型只回一个小 JSON：
  `{"continue": true/false, "event", "location", "emotion", "energy_rate", "minutes"}`。
  `continue=true` 就接着做手上的事（并回上一段，不拆成两条），`false` 就换一件，
  长短她自己定（15~180 分钟，睡觉可跨午夜最长 12 小时）。
- `daily_schedule` 现在存的是「她今天真实过出来的段」，一段一段累积；读的时候按
  「现在」截断，列表里永远没有还没发生的未来。

### 每 15 分钟一个决策窗，生成或不生成是概率的

- 后台每 30 秒问一次「该不该决定下一段」，由日程服务自己判断：
  - **这一段过完了** → 必须重新决定；
  - **身体跟手上的事明显打架**（饿到发慌还在做事、困到不行还醒着）→ 当场重新决定，
    不等这一段结束；
  - 其余情况按 `schedule_change_chance`（默认 30%）掷骰子：掷中就重排（她临时改主意），
    没掷中就接着做手上的事，**一次模型都不调**。
- 同一个决策窗内只掷一次骰子，结果本窗保持不变：30 秒一查的后台循环不会反复掷、也不会
  在一个 15 分钟窗里重排好几次。
- 睡着的那一段**从不重掷、不越线打断**：一觉睡到这段结束为止，夜里不会被吵醒。

### 跨夜与跨天

- 睡觉跨过半夜：今天这半截截到 24:00，剩下的结转到明天（`segment_carry`），零点自动
  接成明天的第一段，**不需要再问一次模型**。睡眠窗口、起床点按完整的一觉算。
- 跨天时今天的段作废，明天从新的一次决定开始；新一天第一段的 prompt 会带上「昨天的收尾」
  作为上下文，不存在半夜把明天排满。

### 过程系统跟着段走

- 「当前过程」不再自成一套时间线：她此刻在做什么以日程的当前段为准。同一件事续了一段
  过程不重开（只把锚点和结束时间对到新段上），换了事就现开一个新过程——过程始终描述的是
  「她此刻这段日程」的详细行为过程。
- 段与段之间的短空档只排一小段过渡，不再凭空编一个几十分钟的大过程。

### 兼容与配置

- **旧版整表日程无缝升级**：老状态里 00:00→24:00 的整天表，读的时候把还没到的时段丢掉，
  剩下的就当她今天已过出来的段，升级当天不断档。
- 模型不可用 / 未配置 / 退避期内：按身体与钟点**本地现算一段**（不留白，也不砸模型）；
  退避一过就让模型接手。解析失败同样进退避，不会每 30 秒砸一次模型。
- 新增配置 `schedule_change_chance`（变动概率 %，默认 30）；`schedule_refresh_minutes`
  改成「决策窗间隔」；`schedule_max_slots` 改成「当天最多保留多少段」。
- 删掉整表时代的内置备用日程模板（`data/schedule_templates.py`）与睡眠对齐函数
  （`align_sleep_to_night`）——作息由她自己排的那一段决定。
