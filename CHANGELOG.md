# 更新日志

只记当前版本。更早的改动看 git 历史或 Release 页。

## v2.16.2 — 上下文里只留事实；地名怎么写都认得；俄罗斯那一栏按 GeoNames 重核

这一版纠正一个方向性错误，再收尾两件核实工作。

README 第一屏就写着「这个插件负责给 AI 一具身体，不负责替 AI 说话」，而注入层里干的正是
替她说话：夜间直接给出「我现在需要休息，明天再聊吧」让她照着发，形式块写着「这一轮只说
一两句，控制在20字上下」，情绪偏了要附一句「说话会短、会顶回去」，语气表写着「语气柔和，
略带撒娇」。v2.15.1 只是把这些句子换个不那么像命令的说法——**问题不在措辞，在于往上下文里
塞句子本身**：模型不会把塞进去的东西当参考，只会当必须执行的任务。那就不再是参考，而是
替她说话、替她思考、替她决定回复多长；而且她睡着时被插件要求「必须 20 字以内回复」这件事
本身就不成立——插件既拦不住消息，也不该规定她怎么回。

### 注入层：只剩事实

`humanoid/prompt_builder.py` 重写。三档 `inject_activity_context` 现在只决定**给多少生活
事实**，不再决定给多少指导：

- `low`（默认）：`【时间】`（日期、星期、几点、上午/晚上、她在哪个城市、群聊还是私聊）、
  `【称呼】`（你管TA叫谁）、`【刚刚】`（距上次说话多久、TA上一句说的是什么）。
- `full`：再加 `【今天】`（此刻在做什么、刚做过什么、接下来做什么）、`【TA说过】`（对方
  之前说过的原话）、`【天气】`。
- `mood_only`：只给一个关系档位词与称呼。

删掉的东西（都不留开关，因为开关本身就是「想塞就塞」的口子）：

| 删掉 | 原来长什么样 |
|---|---|
| 体感句 | `【我的感觉】眼皮很沉，句子不想写长` |
| 形式约束 | `【话的份量】这一轮只说一两句，控制在20字上下`、`别超过60字`、`别展开成长篇` |
| 夜间语气与台词 | `night_mode_force_sleep` 配置项整个删除；夜间窗口保留，但只驱动身体 |
| 情绪态度句与语气表 | `_emotion_lines`、`MOOD_TONE_HINTS` 全删；情绪照旧计算、照旧影响契约 |
| 行为倾向 | `BehaviorService.compute_agency` 删除（注入里那句「更主动、更好奇」就是它） |
| 元指令 | `BOUNDARY_LINE`/`PERMISSION_LINE` 那两句「这些是你的身体…你自己定」也删了 |
| 给人看的技术量 | `UTC+08:00`、`社交能量100%`、`好感46.0/100` 不再出现在上下文里 |

身体与生活并没有消失：它们仍在 `state.json`、仍在联动契约（`body`/`form`/`feelings`/`day`
照旧导出给「自主拟人社交」，那边决定要不要开口与说多长）、仍在 `/你的状态`、`/查看日程`、
`/好感度`、`/拟人诊断` 里给人看。她困不困、说多长、要不要顶回去，是模型从「凌晨两点」
「她刚跑完一整天」这些事实里自己幻想出来的。

实测体积：low ≈ 52 token、full ≈ 69、mood_only ≈ 20（v2.15 是 260~310）。字符上限跟着
收紧到 220 / 620 / 60。

### 时区：三个国家的地区全部收录，并且不许静默退化

- **不再静默退回 `Asia/Shanghai`**：机器缺 tzdata 时，旧逻辑会让「东京的她」过北京时间的
  一天。现在退回 AstrBot 所在机器的本地时间并说明原因。
- **填错与缺库分开报**：名字拼错说「这个时区名用不了（可能拼错了）」，只有真缺 tzdata 才
  提示装库——以前两种情况共用一句提示，把人往错方向引。
- **IANA 名可以直接填**，而且她说出来的是中文地名：填 `Europe/Volgograd`，上下文里是
  「你在伏尔加格勒」；填 `广东省` 就是「你在广东」。
- **换城市会把旧账重新起算**（`core_instance._rebase_after_move`）：`last_update` 与
  `_last_weather_fetch` 存的是墙上时间字符串，读回来附的却是新时区，从东京改到北京会凭空
  多出几小时的精力漂移。
- **身体积分用她的钟点**：`soma._moment_at(epoch)` 把 epoch 换算到她的城市时区——
  `datetime.fromtimestamp(epoch)` 拿到的是机器本地小时，她在别的国家时「此刻在不在睡」会算错。
- 契约 `time.utc_offset_minutes` 算不出时导出 `None` 而不是 `0`（当成 0 等于把她的城市当
  UTC），并新增 `time.tz`。`/拟人诊断` 新增【时间与时区】段；`/时间` 认不出城市时照样给
  时间，但附上原因。

### 地名：她怎么写都认得

表里存的是裸名，而人填配置不会只填裸名。现在先精确匹配，再去掉行政后缀（市/区/县/省/府/都/
盟/地区/自治州/自治区/特别行政区），最后才试前缀。「州」与「道」故意不进后缀表——「北海道」
剪成「北海」会匹到广西的北海市，那是跨国家错时区。

- 认得了：`广东省`、`大阪市`、`东京都`、`北海道`、`内蒙古自治区`、`延边朝鲜族自治州`、`埼玉市`。
- 高一级地名进表：中国 34 个省级行政区、日本 47 个都道府县（官方名与县厅所在地）、俄罗斯与
  首府不同名的联邦主体名（楚瓦什、莫尔多瓦、马里埃尔、汉特-曼西、卡巴尔达-巴尔卡尔…）。
- 重名有结论：`松江` 既是上海的区也是岛根县厅所在地——裸名归松江市（日本），中国的写
  `松江区` / `上海松江`。

### 俄罗斯那一栏：按 GeoNames RU.txt 的 PPLA/PPLA2 记录逐条重核

- **改掉 5 个走快一小时的城镇**：`滕达`、`涅留恩格里`、`阿尔丹`、`奥廖克明斯克` 从
  `Asia/Khandyga` 改回 `Asia/Yakutsk`，`恰拉` 改回 `Asia/Chita`。IANA 给 `Asia/Khandyga`
  的注释只有「Tomponsky, Ust-Maysky」两个区，旧表把这一片都算进去，等于她们的钟快一小时。
- **补上漏掉的首府**：`比利比诺`（`Asia/Anadyr`）、`希姆基`（莫斯科州）、`沃洛格达`、
  `大诺夫哥罗德`、`普斯科夫`。
- **修掉两条 tzdata 里根本不存在的 IANA 名**（`Europe/Cheboksary`、`Europe/Usinsk`）：这种
  名字不抛错，只让那个城市静默退回机器时间；现在按 `Europe/Moscow` 走，并由
  `test_every_entry_is_a_loadable_zone` 逐项挡住。
- 现在 **781 条**：中国 452、俄罗斯 205（27 个 IANA 区）、日本 124。中国大陆（含新疆、西藏）
  统一按北京时间——铁路、电视、手机都这么走；真想让她按当地 UTC+6 过，把配置填成
  `Asia/Urumqi`。

### 测试

- **修掉 2.16.1 遗留的 4 项红测试**：`tests/test_v215_life.py` 的 `EmotionLineTest` 缺
  `import asyncio`（三项 NameError），`DayNarrativeTest` 还在断言已经删掉的「手上在做的：」。
  `py_compile` 与 `compileall` 都查不出漏 import——只有真跑才炸。
- 新增 `tests/test_v216_facts.py`：三档上下文在极端身体状态下仍不含任何命令式句子、体感句、
  字数要求与技术量（`UTC+`、`%`、IANA 代码）；中国 333 个地级行政区**全名**逐项、日本 47
  都道府县官方名与县厅所在地逐项、俄罗斯 85 个联邦主体首府与 12 个远北城镇逐项。
- 全套 **306 项**（24 项为 `main.py` 集成测试）；插件入口冒烟 26 项、Core↔Social 接缝冒烟
  15 项另跑。

### 升级后会有什么不一样

- 她的回复不再被插件压成短句、夜里不再被赶去休息。**如果以前那些效果你觉得「像人」，现在
  该把它写进 AstrBot 的人设里**——那是她自己的性格，而不是插件塞进上下文的句子。
- 上下文小了约 5~6 倍（low 档从 ≈260 token 到 ≈52），长对话里累积的注入噪声随之消失。
- `night_mode_force_sleep` 从配置面板消失（AstrBot 会清掉旧值）；其余配置项一律保留原值。
